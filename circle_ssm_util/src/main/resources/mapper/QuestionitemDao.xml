<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.geminix.circle.util.dao.QuestionitemDao">

    <resultMap type="top.geminix.circle.util.entity.Questionitem" id="QuestionitemMap">
        <result property="itemId" column="itemId" jdbcType="INTEGER"/>
        <result property="questionId" column="questionId" jdbcType="INTEGER"/>
        <result property="itemContent" column="itemContent" jdbcType="VARCHAR"/>
        <result property="isRight" column="isRight" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="QuestionitemMap">
        select
        itemId, questionId, itemContent, isRight
        from circle_blue.questionitem
        where itemId = #{itemid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="QuestionitemMap">
        select
        itemId, questionId, itemContent, isRight
        from circle_blue.questionitem
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="QuestionitemMap">
        select
        itemId, questionId, itemContent, isRight
        from circle_blue.questionitem
        <where>
            <if test="itemid != null">
                and itemId = #{itemid}
            </if>
            <if test="questionid != null">
                and questionId = #{questionid}
            </if>
            <if test="itemcontent != null and itemcontent != ''">
                and itemContent = #{itemcontent}
            </if>
            <if test="isright != null">
                and isRight = #{isright}
            </if>
        </where>
    </select>

    <!--查询所有-->
    <select id="queryAll" resultMap="QuestionitemMap">
        select
        itemId, questionId, itemContent, isRight
        from circle_blue.questionitem
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="itemid" useGeneratedKeys="true">
        insert into circle_blue.questionitem(questionId, itemContent, isRight)
        values (#{questionid}, #{itemcontent}, #{isright})
    </insert>

    <insert id="insertBatch" keyProperty="itemid" useGeneratedKeys="true">
        insert into circle_blue.questionitem(questionId, itemContent, isRight)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.questionid}, #{entity.itemcontent}, #{entity.isright})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="itemid" useGeneratedKeys="true">
        insert into circle_blue.questionitem(questionId, itemContent, isRight)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.questionid}, #{entity.itemcontent}, #{entity.isright})
        </foreach>
        on duplicate key update
        questionId = values(questionId) , itemContent = values(itemContent) , isRight = values(isRight)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update circle_blue.questionitem
        <set>
            <if test="questionid != null">
                questionId = #{questionid},
            </if>
            <if test="itemcontent != null and itemcontent != ''">
                itemContent = #{itemcontent},
            </if>
            <if test="isright != null">
                isRight = #{isright},
            </if>
        </set>
        where itemId = #{itemid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from circle_blue.questionitem where itemId = #{itemid}
    </delete>

</mapper>