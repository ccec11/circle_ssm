<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.geminix.circle.util.dao.NewsinfoDao">

    <resultMap type="top.geminix.circle.util.entity.Newsinfo" id="NewsinfoMap">
        <result property="newsId" column="newsId" jdbcType="INTEGER"/>
        <result property="userId" column="userId" jdbcType="INTEGER"/>
        <result property="newsTitle" column="newsTitle" jdbcType="VARCHAR"/>
        <result property="newsContent" column="newsContent" jdbcType="VARCHAR"/>
        <result property="newsImagePath" column="newsImagePath" jdbcType="VARCHAR"/>
        <result property="newsStatus" column="newsStatus" jdbcType="INTEGER"/>
        <result property="releaseDate" column="releaseDate" jdbcType="OTHER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="NewsinfoMap">
        select
        newsId, userId, newsTitle, newsContent, newsImagePath, newsStatus, releaseDate
        from circle_blue.newsinfo
        where newsId = #{newsid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="NewsinfoMap">
        select
        newsId, userId, newsTitle, newsContent, newsImagePath, newsStatus, releaseDate
        from circle_blue.newsinfo
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="NewsinfoMap">
        select
        newsId, userId, newsTitle, newsContent, newsImagePath, newsStatus, releaseDate
        from circle_blue.newsinfo
        <where>
            <if test="newsid != null">
                and newsId = #{newsid}
            </if>
            <if test="userid != null">
                and userId = #{userid}
            </if>
            <if test="newstitle != null and newstitle != ''">
                and newsTitle = #{newstitle}
            </if>
            <if test="newscontent != null and newscontent != ''">
                and newsContent = #{newscontent}
            </if>
            <if test="newsimagepath != null and newsimagepath != ''">
                and newsImagePath = #{newsimagepath}
            </if>
            <if test="newsstatus != null">
                and newsStatus = #{newsstatus}
            </if>
            <if test="releasedate != null">
                and releaseDate = #{releasedate}
            </if>
        </where>
    </select>

    <!--查询所有-->
    <select id="queryAll" resultMap="NewsinfoMap">
        select
        newsId, userId, newsTitle, newsContent, newsImagePath, newsStatus, releaseDate
        from circle_blue.newsinfo
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="newsid" useGeneratedKeys="true">
        insert into circle_blue.newsinfo(userId, newsTitle, newsContent, newsImagePath, newsStatus, releaseDate)
        values (#{userid}, #{newstitle}, #{newscontent}, #{newsimagepath}, #{newsstatus}, #{releasedate})
    </insert>

    <insert id="insertBatch" keyProperty="newsid" useGeneratedKeys="true">
        insert into circle_blue.newsinfo(userId, newsTitle, newsContent, newsImagePath, newsStatus, releaseDate)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userid}, #{entity.newstitle}, #{entity.newscontent}, #{entity.newsimagepath},
            #{entity.newsstatus}, #{entity.releasedate})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="newsid" useGeneratedKeys="true">
        insert into circle_blue.newsinfo(userId, newsTitle, newsContent, newsImagePath, newsStatus, releaseDate)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userid}, #{entity.newstitle}, #{entity.newscontent}, #{entity.newsimagepath},
            #{entity.newsstatus}, #{entity.releasedate})
        </foreach>
        on duplicate key update
        userId = values(userId) , newsTitle = values(newsTitle) , newsContent = values(newsContent) , newsImagePath =
        values(newsImagePath) , newsStatus = values(newsStatus) , releaseDate = values(releaseDate)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update circle_blue.newsinfo
        <set>
            <if test="userid != null">
                userId = #{userid},
            </if>
            <if test="newstitle != null and newstitle != ''">
                newsTitle = #{newstitle},
            </if>
            <if test="newscontent != null and newscontent != ''">
                newsContent = #{newscontent},
            </if>
            <if test="newsimagepath != null and newsimagepath != ''">
                newsImagePath = #{newsimagepath},
            </if>
            <if test="newsstatus != null">
                newsStatus = #{newsstatus},
            </if>
            <if test="releasedate != null">
                releaseDate = #{releasedate},
            </if>
        </set>
        where newsId = #{newsid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from circle_blue.newsinfo where newsId = #{newsid}
    </delete>

</mapper>